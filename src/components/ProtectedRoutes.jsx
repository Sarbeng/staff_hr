import {useAuthUser} from "react-auth-kit";
const ProtectedRoute = () => {
    // what i will do here is
    // 1. i will check if the user is logged in first
    // 2. if not logged in, redirect o first loginPage
    // then check the role of the user and display the appropriate pages
}